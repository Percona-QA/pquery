#
SET(BINARY_NAME "pquery${vMajor}-${PQUERY_EXT}")
ADD_SUBDIRECTORY(third_party)
INCLUDE_DIRECTORIES(third_party/inih++)
SET(LIBS_TO_LINK ${LIBS_TO_LINK} iniconf)
SET(SOURCES_COMMON pquery.cpp help.cpp node.cpp thread.cpp)
#
IF (MYSQL_FOUND)
  SET(SOURCES_MYSQL "")
ENDIF(MYSQL_FOUND)
#
ADD_EXECUTABLE(${BINARY_NAME} ${SOURCES_COMMON} ${SOURCES_MYSQL} ${SOURCES_PGSQL})
TARGET_LINK_LIBRARIES( ${BINARY_NAME} ${LIBS_TO_LINK} )
INSTALL(TARGETS ${BINARY_NAME} DESTINATION bin)
