IF (MYSQL_FOUND)
  SET (BINARY_NAME "pquery${vMajor}-${PQUERY_EXT}")
  ADD_SUBDIRECTORY(third_party)
  INCLUDE_DIRECTORIES(third_party/inih++)
  ADD_EXECUTABLE(${BINARY_NAME} pquery.cpp help.cpp node.cpp thread.cpp)
  TARGET_LINK_LIBRARIES( ${BINARY_NAME} ${LIBS_TO_LINK} iniconf)
  INSTALL(TARGETS ${BINARY_NAME} DESTINATION bin)
ENDIF(MYSQL_FOUND)
