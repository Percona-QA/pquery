IF (MYSQL_FOUND)
  IF (MARIADB)
    INCLUDE_DIRECTORIES( ${MYSQL_INCLUDE_DIR} ${MYSQL_INCLUDE_DIR}/.. )
  ELSE()
    INCLUDE_DIRECTORIES( ${MYSQL_INCLUDE_DIR} )
  ENDIF(MARIADB)
  ADD_EXECUTABLE(pquery-${PQUERY_EXT} pquery.cpp help.cpp)
  TARGET_LINK_LIBRARIES( pquery-${PQUERY_EXT} ${MYSQL_LIBRARY} ${OTHER_LIBS} )
  INSTALL(TARGETS pquery-${PQUERY_EXT} DESTINATION bin)
ENDIF(MYSQL_FOUND)

