REMOVE_DEFINITIONS(-std=gnu++11)
#
IF( ${ARCH} STREQUAL "x86_64" )
  ADD_DEFINITIONS(-fPIC)
ENDIF( ${ARCH} STREQUAL "x86_64" )
#
SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fvisibility=hidden")
ADD_LIBRARY(iniconf STATIC ini.c INIReader.cpp)
