IF (MYSQL_FOUND)
  SET (BINARY_NAME "pquery2")
  ADD_SUBDIRECTORY(third_party)
  INCLUDE_DIRECTORIES(third_party/inih++/include)
  IF (MARIADB)
    INCLUDE_DIRECTORIES( ${MYSQL_INCLUDE_DIR} ${MYSQL_INCLUDE_DIR}/.. )
  ELSE()
    INCLUDE_DIRECTORIES( ${MYSQL_INCLUDE_DIR} )
  ENDIF(MARIADB)
  ADD_EXECUTABLE(${BINARY_NAME}-${PQUERY_EXT} pquery.cpp help.cpp node.cpp thread.cpp)
  TARGET_LINK_LIBRARIES( ${BINARY_NAME}-${PQUERY_EXT} ${MYSQL_LIBRARY} ${LIBS_TO_LINK} iniconf)
  INSTALL(TARGETS ${BINARY_NAME}-${PQUERY_EXT} DESTINATION bin)
ENDIF(MYSQL_FOUND)
